# Add one name/input_path pair per approved runtime. Each path points to a
# separate YAML under input/serving_runtimes/redhat/ and is relative to the
# repository root. See the unreferenced runtime-template.yaml for the shape.
# Empty values are intentional; generation fails until this is filled in.
source: ""
serving_runtimes:
  - name: ""
    input_path: ""
